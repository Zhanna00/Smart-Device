<ul class="contacts {{ bemMix }}">
	{% for contact in contacts %}
		<li class="contacts__item">
			<a class="contacts__link contacts__link--{{ contact.id }}" href="{{ contact.url }}">
				<span class="contacts__text">{{ contact.text }}</span>
			</a>
		</li>
	{% endfor %}
</ul>
